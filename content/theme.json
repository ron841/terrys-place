{
  "_note": "Token overrides — intentionally empty. terrys.css is the source of truth for design tokens. Slot exists per CMS_SCHEMA convention so a future client-facing CMS can expose minor color/typography overrides without a deploy."
}
